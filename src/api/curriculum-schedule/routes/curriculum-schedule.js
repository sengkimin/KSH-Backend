'use strict';

/**
 * curriculum-schedule router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::curriculum-schedule.curriculum-schedule');
