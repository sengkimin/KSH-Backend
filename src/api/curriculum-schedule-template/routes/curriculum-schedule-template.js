'use strict';

/**
 * curriculum-schedule-template router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::curriculum-schedule-template.curriculum-schedule-template');
