'use strict';

/**
 * salary-resident controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::salary-resident.salary-resident');
