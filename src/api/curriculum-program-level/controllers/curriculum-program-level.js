'use strict';

/**
 * curriculum-program-level controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::curriculum-program-level.curriculum-program-level');
