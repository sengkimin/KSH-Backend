'use strict';

/**
 * curriculum-program-level service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::curriculum-program-level.curriculum-program-level');
