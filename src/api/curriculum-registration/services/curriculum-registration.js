'use strict';

/**
 * curriculum-registration service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::curriculum-registration.curriculum-registration');
