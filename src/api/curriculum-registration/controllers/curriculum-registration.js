'use strict';

/**
 * curriculum-registration controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::curriculum-registration.curriculum-registration');
