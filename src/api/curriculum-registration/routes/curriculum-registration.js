'use strict';

/**
 * curriculum-registration router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::curriculum-registration.curriculum-registration');
