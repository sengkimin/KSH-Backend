'use strict';

/**
 * resident-document controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::resident-document.resident-document');
